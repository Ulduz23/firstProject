server:
  servlet:
    context-path: /api/market-place
    application-display-name: Market Place

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/market_place
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
